# NY Metro Ops (Prototype)

Live LaGuardia ATIS dashboard with a simple runway-flow diagram.

## Run locally

```bash
pip install -r requirements.txt
uvicorn backend.app:app --reload
```

Then open:

`http://127.0.0.1:8000`
